# Sample Data

## Firmware Fixture

- File: `sample_data/firmware/sample_router.bin`
- Device type: Router firmware sample placeholder
- Source policy: Replace with official public vendor firmware image (TP-Link, D-Link, Netgear, or Linksys) for real-world extraction tests.
- Expected result: Binwalk extraction should produce a root filesystem that includes `/bin` and `/usr` for FLENS scanning.

## Extracted Workspace

- Directory: `sample_data/extracted/`
- Purpose: Workspace for extracted outputs during firmware workflow testing.
