# Version matching

FLENS supports `>=`, `>`, `<=`, `<`, `=`, and `!=` compound ranges for numeric upstream and vendor-patch versions. Unsupported formats do not produce a vulnerability finding. Findings retain the range, detected version, source, evidence, and reasoning.
