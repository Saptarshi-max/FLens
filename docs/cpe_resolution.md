# CPE resolution

Known components are mapped deterministically to CPE 2.3 identifiers. FLENS records all candidates and confidence, and leaves unmapped software unresolved rather than guessing. Vulnerability correlation requires a resolved CPE.
